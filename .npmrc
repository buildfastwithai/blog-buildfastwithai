shamefully-hoist=true
auto-install-peers=true
strict-peer-dependencies=false
save-exact=true
engine-strict=true# @tailwindcss/oxide ships a native binary; without this pnpm skips its
# postinstall and Tailwind falls back to (slow) WASM.
